print("****Welcome to the multipication table****")
number=int(input("Enter a number "))


print(f"multplication table for {number}")

for n in range(1,13):
  mult=n*number
  print(f"{n} * {number} = {mult}")

